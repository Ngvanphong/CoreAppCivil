
@{
    ViewData["Title"] = Model.ResultPagging.PageIndex == 1 ? "Bài viết cùng chủ đề" : "Bài viết cùng chủ đề-trang " + Model.ResultPagging.PageIndex;
    ViewData["MetaDescription"] = Model.ResultPagging.PageIndex == 1 ? "Nội dung bài viết cùng chủ đề" : "Nội dung bài viết viết cùng chủ đề-trang " + Model.ResultPagging.PageIndex;
    ViewData["MetaKeyword"] = Model.ResultPagging.PageIndex == 1 ? "Bài viết cùng chủ đề" : "Bài viết cùng chủ đề-trang " + Model.ResultPagging.PageIndex;
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model BlogTagIndexViewModel
